# Project-specific R8 / ProGuard rules.
#
# Most Flutter plugins ship consumer rules in their AARs (MapLibre, Play
# Services Location via geolocator, sqflite, etc.), so this file is empty
# by default. Add `-keep` rules here if a release build hits a runtime
# ClassNotFoundException or NoSuchMethodException for something loaded via
# reflection or JNI.
